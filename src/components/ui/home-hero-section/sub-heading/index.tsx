import { cn } from "@/utils/tailwind/tw-merge";

const SubHeading = () => {
    return (
        <p className={cn("font-semibold text-sm text-center mt-7 md:text-lg")}>
            Dive in now and explore now!
        </p>
    );
};

export default SubHeading;
