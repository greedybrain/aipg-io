import { cn } from "@/utils/tailwind/tw-merge";

const MarqueeLabel = () => {
    return (
        <p className={cn("font-bold text-2xl w-full")}>
            EXPANSIVE AI DIRECTORY
        </p>
    );
};

export default MarqueeLabel;
